name: DESATIVADO - Exportar Short para download V1

on:
  workflow_dispatch:

jobs:
  noop:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow V1 obsoleto. Mantida a versão exportar_short_download_v2.yml."
