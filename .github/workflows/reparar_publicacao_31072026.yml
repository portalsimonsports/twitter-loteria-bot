name: DESATIVADO - Reparar publicação diária de 31/07/2026

on:
  workflow_dispatch:

jobs:
  noop:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Reparo histórico de 31/07/2026 desativado para evitar execução acidental."
