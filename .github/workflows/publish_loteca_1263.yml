name: DESATIVADO - Referência legada da Loteca 1263

on:
  workflow_dispatch:

jobs:
  noop:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow histórico da Loteca 1263 desativado para evitar execução acidental."
